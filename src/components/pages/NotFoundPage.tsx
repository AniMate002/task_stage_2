import React from 'react';
import NotFoundElement from '../atoms/NotFoundElement/NotFoundElement';

const NotFoundPage: React.FC = () => {
    return <NotFoundElement text="404: This page is not found" />;
};

export default NotFoundPage;
